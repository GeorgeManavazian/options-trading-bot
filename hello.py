# hello.py — my very first Python file
# Run it from the project folder with:  .venv/bin/python hello.py

print("The wheel bot lives. Session 2 is underway.")

# A tiny taste of what's coming: Python does math for us.
contracts = 1
shares_per_contract = 100
print("1 options contract controls", contracts * shares_per_contract, "shares.")
